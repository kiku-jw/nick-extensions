{
  "name": "@nick/inkshade",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "bootstrap": "node scripts/bootstrap.mjs",
    "build": "node scripts/build.mjs",
    "lint": "npm --prefix upstream run lint",
    "test": "npm test --prefix upstream"
  }
}
